{
  "scripts": {
    "stat-pages": "bin/stat-pages.sh 'docs/_data/git' 'docs/journal/*/index.*'",
    "serve": "cd docs && bundle exec jekyll serve",
    "build": "cd docs && bundle exec jekyll build --trace"
  }
}
